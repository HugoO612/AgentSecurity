# Agent Security External Messaging (Single Source)

- Agent Security currently supports local isolated runtime mode.
- Agent is not directly installed into Windows host runtime.
- High-risk actions are only executed through controlled bridge actions.
- Missing dedicated runtime is automatically prepared by the formal installer flow.
- Recovery guidance is productized with clear impact and duration summaries.
